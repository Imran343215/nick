{
  "name": "mobile-repair-shop",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile repair shop landing page with a MongoDB backend and query system.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "seed": "node --env-file=.env.local scripts/seed.mjs",
    "seed:demo": "node --env-file=.env.local scripts/seed.mjs --demo"
  },
  "dependencies": {
    "@clerk/nextjs": "^7.8.0",
    "mongoose": "^8.9.4",
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "stripe": "^22.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
